pipeline {
    agent {
        label 'main'
    }
    stages{
        stage('Clone repository') {
            echo 'Clonando...'
        }            
        stage('Build') {
            echo 'Buildando...'
        }
    	stage('Push image') {
            echo 'Pushando...'
        }         
        }
}
